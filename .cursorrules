# Cursor Rules for XRDesignInvestigation

## 关键原则：最小化 Token 消耗

### 1. 编辑策略 — 禁止全文重写
- **必须使用精确替换**：仅修改需要变更的段落/行，不要重写整个文件
- 修改 Markdown 文档时，定位到具体章节，只输出需要改动的部分
- 如果只需要修改一个数字或一句话，不要输出整个文档

### 2. 文件读取策略 — 按需读取
- 先确认需要修改的章节位置（通过搜索关键词）
- 使用行号范围读取，不要一次读取整个大文件
- 本仓库的核心文档通常超过 1000 行（~100KB），全文读取一次约消耗 25,000 tokens

### 3. 回复策略 — 简洁明了
- 不要在回复中复述文档的大段内容
- 修改完成后，只说明改了什么，不要把修改后的全文贴出来
- 如果用户问的是局部问题，只引用相关段落

### 4. Commit 策略
- 每次 commit 写清楚具体改了什么（如 "§2.1 修正PPD表格数据"）
- 不要使用 "Update README.md" 这样的模糊信息

### 5. 文档结构认知
- 本仓库是 XR 设计规范文档集，每个分支通常只有 1-2 个大型 Markdown 文件
- 主要文件：
  - `xr-typography-and-dynamic-layout/README.md` — XR 字体排版与动态布局规范（~1200 行）
  - `xr-eye-adaptation-strategies/README.md` — XR 亮度适应策略（~1000 行）
  - `VAC-DepthZone-Model.md` — VAC 深度分区模型（~1100 行）
- 这些都是纯文档，不需要运行、编译或测试
